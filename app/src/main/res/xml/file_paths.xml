<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Matches Sharing.EXPORT_DIR. Cache-only: nothing else in the app's
         storage is ever exposed through the provider. -->
    <cache-path name="exports" path="export/" />
</paths>
